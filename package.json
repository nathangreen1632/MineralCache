{
  "name": "mineralvendors",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "install": "if [ -d './Server' ] ; then cd Server && npm i ; cd .. ; fi && cd Client && npm i",
    "dev": "concurrently \"npm run dev --prefix Client\" \"npm run dev --prefix Server\"",
    "build": "npm run build --prefix Server && npm run build --prefix Client",
    "start": "npm run start --prefix Server",
    "migrate": "cd Server && NODE_ENV=production npx sequelize-cli db:migrate --config src/config/sequelize.cjs --migrations-path src/migrations --models-path src/models --seeders-path src/seeders",
    "migrate:undo": "cd Server && NODE_ENV=production npx sequelize-cli db:migrate:undo --config src/config/sequelize.cjs",
    "migrate:reset": "cd Server && NODE_ENV=production npx sequelize-cli db:migrate:undo:all --config src/config/sequelize.cjs && NODE_ENV=production npx sequelize-cli db:migrate --config src/config/sequelize.cjs",
    "seed:dev": "cd Server && NODE_ENV=development npx sequelize-cli db:seed:all --env development --config src/config/sequelize.cjs --seeders-path src/seeders",
    "seed:dev:undo": "cd Server && NODE_ENV=development npx sequelize-cli db:seed:undo:all --env development --config src/config/sequelize.cjs --seeders-path src/seeders",
    "seed:prod": "cd Server && NODE_ENV=production npx sequelize-cli db:seed:all --env production --config src/config/sequelize.cjs --seeders-path src/seeders",
    "seed:prod:undo": "cd Server && NODE_ENV=production npx sequelize-cli db:seed:undo:all --env production --config src/config/sequelize.cjs --seeders-path src/seeders"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "keywords": [],
    "author": "One Guy Productions",
    "license": "UNLICENSED",
    "dependencies": {
      "@types/express": "^5.0.3",
      "@types/node": "^24.5.2",
      "express": "^5.1.0",
      "ts-node": "^10.9.2",
      "typescript": "^5.9.2"
  }
}
