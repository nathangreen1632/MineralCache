# --- Identity ---
sonar.organization=nathangreen1632
sonar.projectKey=nathangreen1632_MineralCache
sonar.projectName=MineralCache
sonar.projectVersion=1.0

# --- Monorepo layout (Server + Client) ---
sonar.sources=Client,Server
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/.vite/**,**/.next/**,**/*.spec.ts,**/*.spec.tsx,**/*.test.ts,**/*.test.tsx

# --- TypeScript configs (helps analysis) ---
sonar.typescript.tsconfigPaths=Client/tsconfig.json,Server/tsconfig.json

# --- Tests/Coverage (optional: comment out if you’re not generating these yet) ---
# sonar.javascript.lcov.reportPaths=Client/coverage/lcov.info,Server/coverage/lcov.info
# sonar.tests=Client,Server
# sonar.test.inclusions=**/*.spec.ts,**/*.spec.tsx,**/*.test.ts,**/*.test.tsx

# --- Encoding ---
sonar.sourceEncoding=UTF-8
